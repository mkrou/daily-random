name: Build
on:
  push:
    branches:
      - master

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: get dependencies
        run: yarn
      - name: build
        run: yarn build